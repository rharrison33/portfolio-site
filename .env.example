# URL of the backend API (server/). Defaults to http://localhost:4001 for
# local dev if unset. Set this to your deployed API's URL in production
# (e.g. via your host's environment variable settings) and copy this file
# to .env.local to override it locally.
VITE_API_URL=http://localhost:4001
